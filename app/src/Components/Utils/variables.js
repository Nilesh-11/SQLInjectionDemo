const ServerAddress = "http://localhost:8080";  // Fallback to localhost for testing outside Docker
// const ServerAddress = process.env.REACT_APP_SERVER_ADDRESS || "http://localhost:8080";  // Fallback to localhost for testing outside Docker

export default ServerAddress;